#include <iostream>

int main (){

    // Arithmetic operators return the result of specific arithmetic operations, +, -, + and /.
    //int students = 20;

    //students = students + 2; There's a shorter way of saying this.
    //students+=2
    //students++; // This is an incremental operator and is used to add 1 number.

    // The same goes for subtraction
    // students = students - 2;
    // students-=2;
    //students--;

    //students = students * 2;
    //students*=2;

    //students = students / 3;
    //students/=2
    //students/=3;

    // If our variable was an int variable, the rest from the division would simply be troncated.

    //int remainder = students % 3;

    // The remainder will give us any rest of the students left after the division in groups. 
    // The % module is a great way to find out if a number is even or odd.

    //std::cout << remainder;


    //parenthesis
    //multiplication & division
    //addition & subtraction

    int students = (6 - 5) + 4 * 3 / 2;

    std::cout << students;

    return 0;
}

// I've learnt how to do basic calculation with arithmetic operators in programming.