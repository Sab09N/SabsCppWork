#include <iostream>

int main(){
    const double PI = 3.14159;
    const int LIGHT_SPEED = 10;
    const int WIDTH = 1920;
    const int HEIGHT = 1080;
    // Const is a keyword that allows us to give a predefinite value to a variable in the code. Any other value given to it won't be assigned.
    // Tip: It is convenient to give const variables uppercase letters.

    // assignment: think of a const variable and give it its value.
    
    const double ATM = 1.01325;

    return 0;
}

//I've learnt that the const keyword can be used to give a predefinite value to a variable allowing us to prevent many code errors while we may give different values to the variable for read only.